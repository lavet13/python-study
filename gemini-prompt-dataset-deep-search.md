Find or generate a CSV dataset for a Russian university курсовая работа
on the subject "Разработка прикладных решений на Python".

The курсовая must demonstrate: pandas operations, data visualization,
missing value handling, anomaly detection, clustering, linear regression,
classification (kNN, decision trees, linear classifier), ensemble methods,
gradient boosting, one-hot encoding, feature selection.

Dataset requirements:
- CSV format, comma-separated, headers in English or transliterated Russian
- At least 2000 rows
- Mix of: numeric columns (int and float), categorical columns (3+),
  one date or time column if possible
- Must have: a clear target column for both regression AND classification tasks
- Should have some missing values and outliers (realistic, not clean)
- Domain: suggest from these three options and recommend the best one:
    1. Healthcare — patient records, diagnoses, treatment outcomes
    2. Real estate — property listings, prices, locations, features
    3. Retail/e-commerce — transactions, products, customer segments
  Recommendation criterion: which domain produces the most interesting
  analysis questions across ALL the topics listed above?

For the recommended domain:
- Provide a direct download URL (Kaggle, UCI, data.gov.ru, or similar)
- OR generate a synthetic CSV directly with ~2000 realistic rows
- List 6 specific analysis questions the dataset naturally supports
- Confirm it works for both regression (predict a numeric value)
  and classification (predict a category)
